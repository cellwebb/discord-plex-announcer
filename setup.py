"""Setup script for plex_announcer package."""

from setuptools import setup

# This setup.py file is kept for backward compatibility.
# Configuration is now primarily managed through pyproject.toml

setup()
